This is the online bidding system which is simple version 
how to work it :
   in the compoment folder 
   there is dataloader class in which you can add the data or 
   in the application.propertites in that file spring.jpa.hibernate.ddl-auto=create-drop change it to spring.jpa.hibernate.ddl-auto=update
   then you can add the add the data 
to view data in the database the url is h2concole
in the h2 console url is jdbc:h2:file:./testdb i that you can change the user to admin

remember this applicatin follows the general auction system rules 

that it from my side 

if any changes needed change according to it.
